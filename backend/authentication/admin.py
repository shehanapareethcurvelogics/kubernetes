from django.contrib import admin

# Note: User model is already registered by Django's built-in auth app
# You can access it at: http://localhost:8000/admin/auth/user/
# No need to register it here - it's already available in admin panel

