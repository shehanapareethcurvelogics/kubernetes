apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: django-auth-app
  labels:
    app: postgres
spec:
  type: ClusterIP  # Internal access only
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: postgres

